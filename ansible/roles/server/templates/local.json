{
    "apps": {
        "rose": {
            "id": "{{ rose_id }}",
            "token": "{{ rose_token }}"
        },
        "slack": {
            "token": "{{ slack_token }}"
        },
        "wit": {
            "id": "{{ wit_id }}",
            "token": "{{ wit_token }}"
        }
    }
}
